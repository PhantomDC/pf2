/** * Created by filip on 27/11/2016. */// ==============================================================// !!! JUST FOR TEST, DEMO ETC. !!!// ==============================================================/* Turned on all tooltips */$(function () {    $('[data-toggle="tooltip"]').tooltip()});/* Turned on all popovers */$(function () {    $('[data-toggle="popover"]').popover()});// Enable link to specific tab for tab menusvar url = document.location.toString();if (url.match('#')) {    $('.nav a[href="#' + url.split('#')[1] + '"]').tab('show');}// Change hash for page-reload/*$('.nav a').on('shown.bs.tab', function (e) { window.location.hash = e.target.hash; });*/// toggle buttons in people section(function () {    var btnToggle = $('.js-btn-toggle-tab');    var tab = $('.tab-filter');    var tabMain = $('.tab-filter-main');    btnToggle.on('click', function (e) {        e.preventDefault();        var tabSelected = $(this).add(e.target.hash);        if ($(this).hasClass('active')) {            $(this).removeClass('active');            tabSelected.removeClass('active');            tabMain.addClass('active');        } else {            btnToggle.removeClass('active');            tab.removeClass('active');            $(this).addClass('active');            tabSelected.addClass('active');        }    });})();// Mark a pitch as read and remove new comment indicator$('.js-pitch-1').on('click', function () {    $('.card-block').removeClass('font-weight-sbold');});// hide the comment on click$('.js-pitch-2').on('click', function () {    $('.js-click-hide').css('opacity', '0');});// the chat begins from the bottomfunction getMessagesUp() {    if ($(".chat-body").length > 0) {        $(".chat-body").scrollTop($(".chat-body")[0].scrollHeight);    }}getMessagesUp();// switching between the chats tab-panesvar chatHead = $('.js-list-chats > div:not(:first-child)');var chat = $('.js-chat');chatHead.on('click', function () {    var chatHashSelected = $(this).find('a:first')[0].hash;    var chatSelected = $('.js-chat' + chatHashSelected);    $('.js-list-chats > div').removeClass('active');    $(this).addClass('active');    chat.hide();    chatSelected.show(function () {        getMessagesUp();    });});